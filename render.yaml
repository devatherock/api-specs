services:
  - type: web
    name: openapi-viewer
    env: docker
    plan: free
    autoDeploy: false
    dockerfilePath: ./render/Dockerfile
    dockerContext: ./render
    envVars:
      - key: PORT
        property: port